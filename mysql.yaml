- name: configured mysql server
  hosts: mysql
  become: yes
  tasks:
  - name: install mysql
    ansible.builtin.package:
      name: mysql-server
      state: installed 

  - name: start and enabled mysql server
    ansible.builtin.service:
      name: mysql-server
      state: started
      enabled: yes